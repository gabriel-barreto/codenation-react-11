.navbar
  container()

  align-items center
  display flex
  justify-content space-between
  > .user
    align-items center
    display flex
    > .avatar
      size = 4.8rem
      height size
      object-fit cover
      object-position center
      border-radius 50%
      width size
    > .name
      color white
      font-size 1.4rem
      font-weight 700
      margin 0 0 0 1rem